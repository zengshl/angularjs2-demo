export declare abstract class BaseValidation<ConfigT> {
    config: ConfigT;
    abstract getDefaultConfig(): ConfigT;
    constructor(config: ConfigT);
    protected resolveConfig(preferredConfig: ConfigT, defaultConfig: ConfigT): void;
    private resolveValue<T>(preferredValue, defaultValue);
}
