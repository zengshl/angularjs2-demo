import { EventEmitter } from '@angular/core';
export declare class Message {
    dismissible: boolean;
    onDismiss: EventEmitter<Message>;
    private dismissed;
    private dismiss();
    private classes;
}
