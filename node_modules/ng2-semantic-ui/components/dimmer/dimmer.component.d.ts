import { EventEmitter } from '@angular/core';
export declare class Dimmer {
    classes: boolean;
    isClickable: boolean;
    isDimmed: boolean;
    isDimmedChange: EventEmitter<boolean>;
    private click();
}
