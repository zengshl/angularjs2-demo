import { EventEmitter } from '@angular/core';
import { ControlValueAccessor } from '@angular/common';
export declare class RadioButton {
    classes: boolean;
    name: string;
    value: any;
    isDisabled: boolean;
    isReadonly: boolean;
    private checked;
    private currentValue;
    currentValueChange: EventEmitter<any>;
    private checkedAttribute;
    private isDisabledAttribute;
    onClick(): void;
    update(): void;
    writeValue(value: any): void;
}
export declare class RadioButtonValueAccessor implements ControlValueAccessor {
    private host;
    onChange: () => void;
    onTouched: () => void;
    constructor(host: RadioButton);
    writeValue(value: any): void;
    registerOnChange(fn: () => void): void;
    registerOnTouched(fn: () => void): void;
}
