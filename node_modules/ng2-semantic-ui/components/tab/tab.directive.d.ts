import { EventEmitter } from '@angular/core';
import { Observable } from 'rxjs/Observable';
import { TabContent } from "./tab-content.directive";
export declare class Tab {
    id: string;
    suiTabHeader: string;
    private _content;
    content: TabContent;
    stateChanged$: Observable<Tab>;
    private _stateObserver;
    constructor();
    private _isActive;
    private _isDisabled;
    isActive: boolean;
    isDisabled: boolean;
    private stateObserverNext(change);
    manuallyActivate: boolean;
    manuallyDisable: boolean;
    isActiveChange: EventEmitter<boolean>;
    onActivate: EventEmitter<Tab>;
    private click();
}
