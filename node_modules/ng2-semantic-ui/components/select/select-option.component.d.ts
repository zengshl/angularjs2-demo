import { ElementRef, AfterContentInit } from '@angular/core';
import { Select } from '../select';
export declare class SelectOption implements AfterContentInit {
    private host;
    private el;
    itemClass: boolean;
    value: any;
    HTML: string;
    constructor(host: Select, el: ElementRef);
    ngAfterContentInit(): void;
    click(event: MouseEvent): boolean;
}
