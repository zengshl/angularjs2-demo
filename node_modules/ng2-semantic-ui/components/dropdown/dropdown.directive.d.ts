import { EventEmitter, ElementRef, AfterContentInit } from '@angular/core';
import { DropdownService } from './dropdown.service';
import { DropdownMenu } from './dropdown-menu.directive';
export declare class Dropdown implements AfterContentInit {
    protected _service: DropdownService;
    protected _menu: DropdownMenu;
    isOpen: boolean;
    autoClose: string;
    onToggle: EventEmitter<boolean>;
    isOpenChange: EventEmitter<boolean>;
    isDisabled: boolean;
    constructor(el: ElementRef);
    ngAfterContentInit(): void;
    click(event: MouseEvent): boolean;
}
